import User from "./User.js";
import Flight from "./Flight.js";

class FlightScheduleSystem {
  requestAddFlight(user, flightId, time) {}

  requestModifyFlight(user, flightId, time) {}

  requestDeleteFlight(user, flightId) {}

  getFlightHistory(t1, t2) {}
}

export default FlightScheduleSystem;
