class Location {
  constructor(locationId, isAvailable) {
    this.locationId = locationId;
    this.isAvailable = isAvailable;
  }

  getIsFree() {}

  setIsFree(status) {}
}

export default Location;
