import User from "./User.js";
import RoleEnum from "./RoleEnum.js";

class AccountManager {
  createAccount(admin, id, email, name, password, role) {}

  modifyAccount(admin, id, email, name, password, role) {}

  deleteAccount(admin, userId) {}
}

export default AccountManager;
